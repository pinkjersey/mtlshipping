import { ParentDescriptionPipe } from './parent-description.pipe';

describe('ParentDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new ParentDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
